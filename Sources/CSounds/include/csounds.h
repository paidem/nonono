#include <stddef.h>

// The two mp3 clips, embedded at build time (see sounds.c).
const unsigned char *nonono_wait_mp3(void);
size_t nonono_wait_mp3_len(void);
const unsigned char *nonono_phew_mp3(void);
size_t nonono_phew_mp3_len(void);
